// Variable with var

var product = "Monitor 24 in";
product = 'Montor 19 in';
product = 20;
//console.log('Variable with VAR' + product);

// Initialize no value
var available;
available = true;
available = false;
//console.log('Initialize no value' + available);

// Initialize multiple variables
var category = 'Computers',
    brand = "Famous",
    qualification = 5;  

// Variables cannot initialize with numbers 
//var 99dias;
var dias99;

// Initialize variables with nomenclature
var camelCase;
var PascalCase;
var snake_case