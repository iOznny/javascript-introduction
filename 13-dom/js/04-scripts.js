// Query selector
const card = document.querySelector('.card');
console.log(card);

// We can have specific selectors like in CSS
const info = document.querySelector('.premium .info');
console.log(info);

const secoundCard = document.querySelector('section.hospedaje .card:nth-child(2)');
console.log(secoundCard);

// Select form 
const form = document.querySelector('#formulario');
console.log(form);

const nav = document.querySelector('nav');
console.log(nav);