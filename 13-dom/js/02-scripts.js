// Select elements by his class
const header = document.getElementsByClassName('header');
console.log(header);

const hero = document.getElementsByClassName('hero');
console.log(hero);

// If the classes exist more than one time
const containers = document.getElementsByClassName('contenedor');
console.log(containers);

// If the class dont exits
const dontExists = document.getElementsByClassName('dontExists');
console.log(dontExists);