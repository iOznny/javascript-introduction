// Strings with Methods -> trimStart(), trimEnd() and trim().

const example04 = '     Monitor 20 inches    ';
//console.log(example04);
//console.log(example04.length);

// Delete spaces at start
//console.log('Trim start -> ' + example04.trimStart());

// Delete spaces at end
//console.log('Trim end ->' + example04.trimEnd());

// Join method trim in start and end
//console.log('Trim start and end -> ' + example04.trimStart().trimEnd());

// Delete spaces in both directions
//console.log('Trim both directions -> ' + example04.trim());