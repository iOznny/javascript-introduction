// Object Literal
const product = {
    name: 'Monitor 20 in',
    price: 200,
    available: true
};

// Get keys of the object
console.log('Keys', Object.keys(product));

// Get values of the keys
console.log('Values', Object.values(product));

// Returns an array of eigen pairs
console.log('Entries', Object.entries(product));