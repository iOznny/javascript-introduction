const name = 'Monitor 20 in';
const price = 300;
const available = true;

// Create object literal
const product = {
    name: 'Monitor 20 in',
    price: 200,
    available: true
};

console.log(product);